# W1-A progress ledger — profile + notifications UI
- 2026-09-24 worktree created on feat/profile-notifications-ui from origin/main 38a4907
- 2026-09-24 read specs + gym-be handlers (me.go/photo.go on worktree-profile-photo-streak, notifications.go on main)
- Ruling: photo upload is a RAW body with Content-Type image/jpeg|png (backend photo.go), not multipart; accept only jpeg/png client-side.
- Ruling: CSP img-src lacks blob:, so the authenticated GET /me/photo blob is shown via a FileReader data: URL.
- Ruling: CORS Allow-Methods omits POST but POST is a CORS-safelisted method, so POST /me/photo and /notifications/{id}/read pass preflight; no backend change needed.
- Ruling: polling rides sync.js cadence via a `gym:synctick` DOM event emitted from syncNow() (non-debounce reasons); auth changes via `gym:authchange` emitted from renderAuthUI().
- Ruling: profile form requires all three fields (backend validateProfile rejects missing weight/height).
- Ruling: photos are downscaled client-side to <=1024px JPEG before upload (fallback: original file) so typical phone photos fit the 5 MiB cap.
- Ruling: existing browser push subscriptions are not actively unsubscribed; notifyjob no longer sends push so they are inert. gym_push_endpoint stays in sync.js LOCAL_ONLY and is removed from localStorage once.
- 2026-09-24 plan written: docs/superpowers/plans/2026-09-24-profile-notifications-ui.md
- PAUSED 2026-09-24 21:37 — Task 1 (header.js + plumbing: topBar, sync.js gym:authchange/gym:synctick, calendar gym:workoutcomplete, T keys for all 3 features, CSS, Dockerfile/SW lists) code written + node --check OK, committed as WIP; not yet smoke-tested (jsdom harness installed in scratchpad). Next step: smoke-test header.js, then Task 2 notifications.js, Task 3 profile.js (+ui.js TABS 'profile', #screen-profile), Task 4 push removal, Task 5 PR.
- 2026-09-26 resumed: fetched + merged origin/main (#40 workout builder, #41 Ramadan toggle) into branch; resolved conflicts in Dockerfile/index.html/service-worker.js (kept both workout-builder.js and header.js, added placeholder entries for notifications.js/profile.js). Reviewed WIP header.js: Task 1 is functionally complete (topBar DOM, GymHeader API, gym:authchange/gym:synctick wiring in sync.js, gym:workoutcomplete in calendar.js, CSS, Dockerfile/SW lists, T keys for all 3 features already written) — node --check clean.
- 2026-09-26 Task 4 done: removed web-push opt-in UI from calendar.js/index.html/styles.css/service-worker.js/config.js/docker/40-render-config.sh; comment-only change in sync.js keeping gym_push_endpoint in LOCAL_ONLY. grep -i push/vapid shows only kept entries. Committed 20b975b, pushed.
- 2026-09-26 resumed after usage-limit stop: reviewed WIP header.js/notifications.js/profile.js/ui.js/index.html against the plan (Tasks 1-3) — functionally complete, all T keys (EN+AR) present, node --check clean on all changed JS. Fixed one gap: .prof-signout/.prof-status-err used a new #e88 hex not in the plan's token list; changed to the existing #ff6b6b (.coach-err) for consistency (commit 30e9849, pushed). Full browser smoke test via Playwright against a local static server + route-mocked /me, /notifications, /me/profile: zero console errors across anon load, EN, AR/RTL toggle (MutationObserver re-render confirmed), simulated signed-in state (header streak badge, bell badge, panel open/list/mark-read/close), avatar menu -> profile navigation with correct field prefill, Back button returns to originating tab, client-side validation (weight bounds) shows localized error, PUT /me/profile save round-trip, sign-out clears header+badge. relTime() and validateProfile() pure-helper node smoke tests also pass all boundary cases.
- 2026-09-26 Task 5 done: origin/main unchanged since the 73bb479 merge (nothing new to merge); final verify (node --check all JS, Dockerfile/SW asset-list cross-check, push/vapid grep) clean; pushed 30e9849 (color-token fix) + 14bb239 (ledger); opened PR #42 (Eslam1141/Eslam1141.github.io, feat/profile-notifications-ui -> main). Two non-contiguous "wip:" commits (852547b, e435b5b) left unsquashed per instructions (a merge + unrelated commits sit between them) — noted in PR body, recommend "Squash and merge". STREAM DONE.
