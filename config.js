/* config.js — runtime configuration.
 * In the gym-ui container this file is regenerated at startup from
 * GYM_API_BASE and GOOGLE_CLIENT_ID. The committed copy below keeps the
 * site working from GitHub Pages / file:// with sync disabled. */
window.GYM_API_BASE = "";
window.GOOGLE_CLIENT_ID = "";
